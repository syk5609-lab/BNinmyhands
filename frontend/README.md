# Frontend (Next.js App Router)

## Quick start

```bash
npx create-next-app@latest frontend --ts --tailwind --app
# then copy/merge files from this folder
cd frontend
pnpm install
pnpm dev
```

Set backend URL if needed:

```bash
SCANNER_API_BASE_URL=http://127.0.0.1:8000
```
